name: "Setup and Install Dependencies"
description: "Sets up the project and installs dependencies"

runs:
   using: "composite"
   steps:
      - uses: actions/setup-node@v4
        with:
            node-version-file: ".nvmrc"
            cache: "npm"
      - run: npm ci
        shell: bash
